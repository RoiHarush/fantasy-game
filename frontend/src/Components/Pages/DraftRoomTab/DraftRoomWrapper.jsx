import PlayersWrapper from "../../General/PlayersWrapper";

function DraftRoomWrapper({ players, user, setUser }) {
    return (
        // <PlayersWrapper
        //     user={user}
        //     setUser={setUser}
        //     mode="draft"
        // />

        <h3>The Draft Room is not available yet. This feature will be added in an upcoming update.</h3>
    );
}

export default DraftRoomWrapper;
